language: python

python:
  - 3.4

install:
  - git clone git@github.com:selfcov/selfcov.github.io.git docs/_build
  - pip install -r requirements.txt

script:
  - cd docs && make html

after_success:
  - pwd
